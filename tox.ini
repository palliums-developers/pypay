[tox]
envlist = py36, py37

[testenv]
deps=
	pytest
	coverage
commands=
	coverage run --source=pypay '--omit=pypay/qml/*,pypay/plugins/*,pypay/scripts/*,pypay/tests/*' -m py.test -v
	coverage report
extras=
	tests
